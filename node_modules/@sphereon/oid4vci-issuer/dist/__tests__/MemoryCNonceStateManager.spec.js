"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const oid4vci_common_1 = require("@sphereon/oid4vci-common");
const uuid_1 = require("uuid");
const state_manager_1 = require("../state-manager");
describe('MemoryIssuerStateManager', () => {
    let memoryCNonceStateManager;
    beforeAll(() => {
        memoryCNonceStateManager = new state_manager_1.MemoryStates({ expiresInSec: 0 });
    });
    beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
        const day = 86400000;
        for (const i of Array.from(Array(3).keys())) {
            const timestamp = +new Date(+new Date() + day * (i - 1));
            const cNonce = { cNonce: (0, uuid_1.v4)(), createdAt: timestamp };
            yield memoryCNonceStateManager.set(String(i), cNonce);
        }
    }));
    it('should retrieve a state', () => __awaiter(void 0, void 0, void 0, function* () {
        yield expect(memoryCNonceStateManager.get(String(0))).resolves.toBeDefined();
        yield expect(memoryCNonceStateManager.getAsserted(String(0))).resolves.toBeDefined();
    }));
    it('should check whether a state exists', () => __awaiter(void 0, void 0, void 0, function* () {
        yield expect(memoryCNonceStateManager.has(String(1))).resolves.toBeTruthy();
    }));
    it('should delete a state', () => __awaiter(void 0, void 0, void 0, function* () {
        yield expect(memoryCNonceStateManager.delete(String(1))).resolves.toBeTruthy();
        yield expect(memoryCNonceStateManager.get(String(0))).resolves.toBeDefined();
        yield expect(memoryCNonceStateManager.get(String(1))).resolves.toBeUndefined();
        yield expect(memoryCNonceStateManager.get(String(2))).resolves.toBeDefined();
    }));
    it('should delete all expired states', () => __awaiter(void 0, void 0, void 0, function* () {
        yield memoryCNonceStateManager.clearExpired(+new Date() + 10000);
        // yesterday should be expired
        yield expect(memoryCNonceStateManager.get(String(0))).resolves.toBeUndefined();
        // today should be expired because the method parameter is a few milliseconds ahead
        yield expect(memoryCNonceStateManager.get(String(1))).resolves.toBeUndefined();
        yield expect(memoryCNonceStateManager.get(String(2))).resolves.toBeDefined();
    }));
    it('should delete all states', () => __awaiter(void 0, void 0, void 0, function* () {
        yield memoryCNonceStateManager.clearAll();
        yield expect(memoryCNonceStateManager.get(String(0))).resolves.toBeUndefined();
        yield expect(memoryCNonceStateManager.get(String(1))).resolves.toBeUndefined();
        yield expect(memoryCNonceStateManager.get(String(2))).resolves.toBeUndefined();
    }));
    it('should throw exception when state does not exist', () => __awaiter(void 0, void 0, void 0, function* () {
        yield expect(memoryCNonceStateManager.getAsserted(String(3))).rejects.toThrowError(Error(oid4vci_common_1.STATE_MISSING_ERROR + ' (3)'));
    }));
});
//# sourceMappingURL=MemoryCNonceStateManager.spec.js.map